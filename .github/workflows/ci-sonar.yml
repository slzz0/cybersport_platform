name: CI Sonar

on:
  push:
    branches:
      - main
      - master
  pull_request:

jobs:
  build-and-sonar:
    name: Build, Test and Sonar
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: maven

      - name: Build, run tests, coverage and Sonar
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: >
          mvn -B clean
          -Dtest=PlayerServiceImplTest,TeamServiceImplTest
          test sonar:sonar
          -Dsonar.token=${SONAR_TOKEN}
